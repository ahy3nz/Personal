#include <iostream>
using namespace std;

int power(int a, int b){
  if(b == 0) {
    return 1;
  }
  else {
    return a*power(a, b-1);
}

int main (){
  int x,y,z;
  cin>>x;
  cin>>y;
  z=power(x,y);
  cout<<x<< " ^ " << y << " = " << z << endl;
  return 0;
}
