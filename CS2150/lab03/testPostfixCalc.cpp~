#include <iostream>
#include <string>
#include "postfixCalculator.h"
using namespace std;

int main() {
  string str = "1 2 +";
  postfixCalculator *calc = new postfixCalculator(str);
  cout << calc->answer() <<endl;
  return 0;
}
